// Production placeholder — all data is served from the API.
// These empty arrays are kept to avoid import errors in any component
// that may still reference them; they will always render empty states.
export const mockCategories = [];
export const mockProducts = [];
export const mockBanners = [];
export const cleaningProductsBanner = [];
